SimpleCalc
==========

Simple string calculator. 

Does't work yet

For build run:

`cabal configure --enable-tests`

`cabal build`

for start testing:
`cabal test`

for running program:
`./dist/build/calc/calc `


